name: Python Package CI

on:
  push:
    branches: [ "main" ] # Runs every time push to main
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest # The robot's operating system

    steps:
    - uses: actions/checkout@v4

---
